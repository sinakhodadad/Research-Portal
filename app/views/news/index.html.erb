<% if !current_user.nil? && (current_user.has_role?("admin") || current_user.has_role?("teacher")) %>
<div class="page-header">
    <%= link_to new_news_path, class: 'btn btn-primary' do %>
      <span class="glyphicon glyphicon-plus"></span>
      Add some news
    <% end %>
    
  </div>
<% end %>

</br></br>

<div class="row">


  <div class="col-md-6">
    <div class="portlet light">
        <div class="portlet-title">
            <div class="caption">
              <i class="icon-users"></i>
              <span class="caption-subject bold uppercase font-green-sharp"> Members </span>
            </div>
            <div>
            </div>
        </div>
        <div class="portlet-body" style="height: auto;">
            <div class="scroller" style="height:300px"
                 data-rail-visible="1" data-rail-color="yellow" data-handle-color="#a1b2bd">
                 <strong class="bold uppercase">The Head of the lab</strong>
            
                 <table class="table table-striped table-bordered table-hover">
                  <thead>
                    <tr>
                      <!-- <th>Name</th> -->
                      <!-- <th>Role</th> -->
                    </tr>
                  </thead>
                
                <div class="form-horizontal">
                    <div class="form-group">

                      <div class="btn-group-img btn-group">
                            
                      <tbody>
<!--                         <%#= content_tag_for(:tr, @admins) do |ad| %>
                            <td><%#= ad.first_name %></td>
                            <td><%#= ad.last_name %></td>
                            <td> Admin </td>
                        <%# end %>
 -->
                        <%= content_tag_for(:tr, @teachers) do |tc| %>
                            <td>
                              <% if not tc.avatar.nil? %>

                                  <img class="img-rounded" width="50" height="50" src="<%= tc.avatar.url(:thumb) %>"/>
                              <% else %>

                                  <img class="img-rounded" width="50" height="50" src="/no_avatar.jpg"/></button>
                              <% end %>
                              DR <%=link_to( tc.full_name , :controller => "profiles", :action => "show", :id => tc.id)%>
                            </td>
                            
                            <%#= tc.first_name %> <%#= tc.last_name %>
                            <!-- <td> Teacher </td> -->
                        <% end %>
<!-- 
                        <%#= content_tag_for(:tr, @students) do |st| %>
                            <td><%#= st.first_name %></td>
                            <td><%#= st.last_name %></td>
                            <td> Student </td>
                        <%# end %>

                        <%#= content_tag_for(:tr, @guests) do |gs| %>
                            <td><%#= gs.first_name %></td>
                            <td><%#= gs.last_name %></td>
                            <td> Guest </td>
                        <%# end %>
 -->
                      </tbody>
                    </div>
                </div>
              </table>

              <strong class="bold uppercase" >Website Admins</strong>
            
                 <table class="table table-striped table-bordered table-hover">
                  <thead>
                    <tr>
                      <!-- <th>Name</th> -->
                      <!-- <th>Role</th> -->
                    </tr>
                  </thead>
                
                <div class="form-horizontal">
                    <div class="form-group">
                      <tbody>
<!--                         <%#= content_tag_for(:tr, @admins) do |ad| %>
                            <td><%#= ad.first_name %></td>
                            <td><%#= ad.last_name %></td>
                            <td> Admin </td>
                        <%# end %>
 -->
                        <%= content_tag_for(:tr, @admins) do |tc| %>

                            <td>
                              <% if not tc.avatar.nil? %>

                                  <img class="img-rounded" width="50" height="50" src="<%= tc.avatar.url(:thumb) %>"/>
                              <% else %>

                                  <img class="img-rounded" width="50" height="50" src="/no_avatar.jpg"/></button>
                              <% end %>
                              <%=link_to( tc.full_name , :controller => "profiles", :action => "show", :id => tc.id)%>
                            </td>
                            
                        <% end %>
<!-- 
                        <%#= content_tag_for(:tr, @students) do |st| %>
                            <td><%#= st.first_name %></td>
                            <td><%#= st.last_name %></td>
                            <td> Student </td>
                        <%# end %>

                        <%#= content_tag_for(:tr, @guests) do |gs| %>
                            <td><%#= gs.first_name %></td>
                            <td><%#= gs.last_name %></td>
                            <td> Guest </td>
                        <%# end %>
 -->
                      </tbody>
                    </div>
                </div>
              </table><strong class="bold uppercase">Students</strong>
            
                 <table class="table table-striped table-bordered table-hover">
                  <thead>
                    <tr>
                     <!--  <th>Name</th>
                      <th>Role</th> -->
                    </tr>
                  </thead>
                
                <div class="form-horizontal">
                    <div class="form-group">
                      <tbody>
<!--                         <%#= content_tag_for(:tr, @admins) do |ad| %>
                            <td><%#= ad.first_name %></td>
                            <td><%#= ad.last_name %></td>
                            <td> Admin </td>
                        <%# end %>
 -->
                        <%= content_tag_for(:tr, @students) do |tc| %>

                            <td>
                              <% if not tc.avatar.nil? %>

                                  <img class="img-rounded" width="50" height="50" src="<%= tc.avatar.url(:thumb) %>"/>
                              <% else %>

                                  <img class="img-rounded" width="50" height="50" src="/no_avatar.jpg"/></button>
                              <% end %>
                              <%=link_to( tc.full_name , :controller => "profiles", :action => "show", :id => tc.id)%>
                            </td>
                            
                           <!--  <td> Teacher </td> -->
                        <% end %>
<!-- 
                        <%#= content_tag_for(:tr, @students) do |st| %>
                            <td><%#= st.first_name %></td>
                            <td><%#= st.last_name %></td>
                            <td> Student </td>
                        <%# end %>

                        <%#= content_tag_for(:tr, @guests) do |gs| %>
                            <td><%#= gs.first_name %></td>
                            <td><%#= gs.last_name %></td>
                            <td> Guest </td>
                        <%# end %>
 -->
                      </tbody>
                    </div>
                </div>
              </table>

              <strong class="bold uppercase">Guests</strong>
            
                 <table class="table table-striped table-bordered table-hover">
                  <thead>
                    <tr>
                      <!-- <th>Name</th>
                      <th>Role</th> -->
                    </tr>
                  </thead>
                
                <div class="form-horizontal">
                    <div class="form-group">
                      <tbody>
<!--                         <%#= content_tag_for(:tr, @admins) do |ad| %>
                            <td><%#= ad.first_name %></td>
                            <td><%#= ad.last_name %></td>
                            <td> Admin </td>
                        <%# end %>
 -->
                        <%= content_tag_for(:tr, @guests) do |tc| %>

                            <td>
                              <% if not tc.avatar.nil? %>

                                  <img class="img-rounded" width="50" height="50" src="<%= tc.avatar.url(:thumb) %>"/>
                              <% else %>

                                  <img class="img-rounded" width="50" height="50" src="/no_avatar.jpg"/></button>
                              <% end %>
                              <%=link_to( tc.full_name , :controller => "profiles", :action => "show", :id => tc.id)%>
                            </td>
                            
                        <% end %>
<!-- 
                        <%#= content_tag_for(:tr, @students) do |st| %>
                            <td><%#= st.first_name %></td>
                            <td><%#= st.last_name %></td>
                            <td> Student </td>
                        <%# end %>

                        <%#= content_tag_for(:tr, @guests) do |gs| %>
                            <td><%#= gs.first_name %></td>
                            <td><%#= gs.last_name %></td>
                            <td> Guest </td>
                        <%# end %>
 -->
                      </tbody>
                    </div>
                </div>
              </table>
            </div>
        </div>
    </div>
  </div>

  <div class="col-md-6">
    <div class="portlet light">
        <div class="portlet-title">
            <div class="caption">
              <i class="icon-pencil"></i>
              <span class="caption-subject bold uppercase font-green-sharp"> Latest Publications</span>
            </div>
        </div>
        <div class="portlet-body" style="height: auto;">
            <div class="scroller" style="height:300px"
                 data-rail-visible="1" data-rail-color="yellow" data-handle-color="#a1b2bd">
                <div class="form-horizontal">
                <p>You can see the publications by clicking on them</p>
            
                <% @publications.each do |publication| %>
                  <hr/>
                  <i class="glyphicon glyphicon-pencil font-green"></i>
                  <span class= "small">

                  <% if publication.document.present? %>
                    <% if publication.users.present? %>
                      <% if publication.users.size == 1 %>
                        <% publication.users.each do |pb|%>
                          <%= pb.last_name %>
                        <% end %>
                        ,
                        <%=link_to publication.title, publication.document.url %>
                      <% else %>
                        <% publication.users.each_with_index do |pb, index|%>
                          <% if index != publication.users.size - 1 %>
                            <%= pb.last_name %>
                            &
                          <% end %>
                        <% end %>
                        <%= publication.users.last.last_name %>,
                        <%=link_to publication.title, publication.document.url %>

                      <% end %>
                    <% else %>
                      <% if publication.users.size == 1 %>
                        <% publication.users.each do |pb|%>
                          <%= pb.last_name %>
                        <% end %>
                        ,
                        <%=link_to publication.title, publication %>
                    
                      <% else %>
                        <% publication.users.each_with_index do |pb, index|%>
                          <% if index != publication.users.size - 1 %>
                            <%= pb.last_name %>
                            &
                          <% end %>
                      <% end %>
                    <% end %>
                    <%= publication.users.last.last_name %>,
                    <%=link_to publication.title, publication %>
                  <% end %>
                <% end %>
                

                  
                , Vol. <%= publication.volume %>, No. <%= publication.id %>, pp. <%= publication.page%>-<%= publication.pageend%>, <%= publication.year %> </span>

               
              <%end %>
                
                </div>
            </div>
        </div>
    </div>
  </div>

  
  <div class="col-md-12">
    <div class="portlet light">
        <div class="portlet-title">
            <div class="caption">
              <i class="icon-speech"></i>
              <span class="caption-subject bold uppercase font-green-sharp"> Latest News</span>
            </div>
            <div>
            </div>
        </div>
        <div class="portlet-body" style="height: auto;">
            <div class="scroller" style="height:300px"
                 data-rail-visible="1" data-rail-color="yellow" data-handle-color="#a1b2bd">
                <div class="form-horizontal">
                  <% @newses.each do |pb| %>
                    <div class="form-group">
                        <label class="col-sm-12 "><strong><%= link_to pb.title, pb %></strong></label>
                        <div class="col-sm-12">
                          <%= pb.body %>
                        </div>
                    </div>
                  <% end %>
                </div>
            </div>
        </div>
    </div>
  </div>

</div>

<!-- <div class=" col-sm-5 table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
        <th>Body</th>
        <th>Title</th>
            <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <%#= content_tag_for(:tr, @news) do |news| %>
          <td><%#= news.body %></td>
          <td><%#= news.title %></td>
          <td><%#= link_to 'Show', news %></td>
          <td><%#= link_to 'Edit', edit_news_path(news) %></td>
          <td><%#= link_to 'Destroy', news, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      <%# end %>
    </tbody>
  </table>
</div>
 -->
